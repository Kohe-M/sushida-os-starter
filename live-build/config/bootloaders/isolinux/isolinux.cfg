# Security: prevent boot parameter editing at the ISOLINUX prompt level.
# These directives are also read by EXTLINUX when the same image boots from a
# USB drive.
ALLOWOPTIONS 0
NOESCAPE 1
PROMPT 0
TIMEOUT 30
include menu.cfg
default vesamenu.c32
