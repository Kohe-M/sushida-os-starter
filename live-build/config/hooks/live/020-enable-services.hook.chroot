#!/usr/bin/env bash
#
# live-build chroot hook -- enable production systemd services.
# Runs inside the target chroot during image build, NOT on the host.
set -euo pipefail

systemctl enable sushida-kiosk.service
